
类作为形参和返回值
方法形参是类，其实需要的是该类的对象
方法的返回值是类，其实返回的是该类的对象


抽象类作为形参和返回值
方法形参是抽象类，其实需要的是该抽象类的子类对象
方法的返回值是抽象类，其实返回的是该抽象类的子类对象


接口作为形参和返回值
方法的形参是接口，其实需要的是该接口的实现类对象
方法的返回值是接口，其实返回的是该接口的实现类对象











---------
虽然现在的我看不懂，但是一定不是我理解问题
换一个老师教授才行
考虑udacity